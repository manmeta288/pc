{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "sh -c 'cd .. && corepack pnpm install --frozen-lockfile && corepack pnpm build'"
  },
  "deploy": {
    "startCommand": "sh -c 'cd .. && export HOST=0.0.0.0 SERVE_UI=true && exec node server/dist/index.js'",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 600,
    "restartPolicyType": "ON_FAILURE"
  }
}
